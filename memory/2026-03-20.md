# 2026-03-20 日记

## 上午工作记录

### 记忆同步问题修复
- 发现 morning-memory-sync cron 只做归档播报，没有拉取 GitHub MEMORY.md 写入本地
- 已修复：改为先 fetch GitHub → 写入本地 MEMORY.md，再播报
- 触发时间从 09:30 改为 09:25

### 归档 cron 时区问题修复
- 四个归档 cron（code/config/numeric/design）均未设置时区
- 导致 21:00 那次从未触发（UTC 21:00 = 北京凌晨 5 点）
- 已全部加上 Asia/Shanghai 时区
- 同时优化 prompt：加 git diff --cached --quiet 判断，有变更才 commit

### 分区规则确认
- 固定五个分区：代码排查、配置编辑、数值讨论、系统设计、UI设计
- 不得自行新增分区，只在哥明确说"拉群了"后才建新分区

### UI设计新分区
- 哥新开 UI 设计群，功能：UI设计 + UX交互逻辑输出 + prefab 生产
- 已建 [UI设计] 分区到 MEMORY.md 并 push GitHub
- 已建 ui-group-archive cron（21:00/00:00，Asia/Shanghai）

### UIMechaRepair prefab 任务
- 新任务类型：根据设计图 + img 资源拼接 Unity prefab
- 学习了项目预制体规则（UILogin 目录下 UITest.prefab）
- 获取了所有 img_mecha_window_* 的 GUID
- 生成了 Editor 脚本 CreateUIMechaRepair.cs，已推送到 E:\A2\X3_client\client\Assets\Editor\
- 待哥启动 Unity 后执行 Tools → UI → Create Mecha Repair Prefab 验收

### 代码排查
- 有人问了一个 PostProcessData.py 报错：ValueError: 2 is not in list
- 根因：SortedOpenAreas 列表里没有 areaID=2，.index() 直接抛异常
- 是数据问题（配表引用了不存在的 areaID），建议加 `if areaID not in list` 防御



## 下午工作记录（15:00-18:00）

### 工作规范更新
- 确认了分区群共用同一 session，本地文件实时共享，无需跨 session 同步
- 跨 session（重启后）依赖 09:25 cron 从 GitHub 拉取最新 MEMORY.md
- 手动触发了一次全分区归档并 push GitHub，清理了临时文件 UIMechaRepair.prefab / UIMechaRepair_final.prefab

### 长任务播报规范确立（重要）
- 平台限制：同一条消息内的过程描述对哥不可见，消息发出时已是完成状态
- 正确做法：执行前先发"开始了，预计 XX 秒"；中间播报用子 agent，子 agent 通过 message 工具直接发到 hub-channel
- 子 agent run 模式验证：跑完不会自动把结果推给用户，需要在 task 里明确要求 message 发送
- 成功跑通了子 agent 实时播报流程（GitHub Trending 搜索任务）
- 系统自动发的 "Subagent main finished" 是平台行为，暂无法关闭

### 任务判断规则
- 超过 10s / SSH / 多步工具调用 → 子 agent + 实时播报
- 每次判断失误（应用子 agent 但没用）事后必须在复盘里记录原因

### gen_i18n 脚本分析
- 脚本以配置表 TXT_ 字段为唯一基准，不读 LocalLanguage
- 触发删除条件：i18n 表里的 key 和中文在配置表字典里都找不到
- 实操避免误删：备份 → 跑脚本 → 检查"已删除"行 → 确认无误再保存
- 此任务耗时约 40s，应用子 agent 但直接自己跑了，判断失误

### 数值设计复盘（SLG 主堡）
- 对比了自己的老设计（69天，偏松）和 Sonnet 设计（91天，精确卡目标）
- 核心差距：时间精度、资源比例、付费卡点、玩家分层、文档质量
- 学习要点：数值设计不只是公式，要有商业化逻辑推导和玩家分层视角

### 新项目立项讨论
- 融合玩法 SLG，小游戏买量 → 有机过渡 SLG，要求有爆点
- 补充了8个新方向，优先推荐：地下钻探、寄生感染、建筑拆解重组
- 哥尚未确认感兴趣方向，待续

