# 2026-03-24

## 重要规则（每次确认后立即同步，禁止遗忘）

- **日报格式**：每条 bullet point 用自然语言概述工作梗概，不写具体文件名/函数名/表名等细节。例如：「核查了某问题，确认是规则文本与配置不一致」而非「核查了 ActvInvasion.xlsx + RuleTips.xlsx ID=1901」。
- **cron 时间**：首次归档 21:00（原来 21:15），日报发送 21:00，23:00/00:30 归档及 GitHub push 维持不变。
- **归档原则**：主 session 工作内容不依赖分区 cron，直接进入主 session 归档；主 session 本身也是归档入口。
- **规则确认即同步**：任何规则、规范、约定一旦确认，立刻写入 MEMORY.md 对应区块，不等下一次 session。

## Jira 提单自动化与规则

- 配置 ~/.env.jira：JIRA_URL=https://jira.tap4fun.com/, JIRA_PERSONAL_TOKEN（用户提供）已写入，JIRA_PROJECT=X3NEW。
- 已安装 python-dotenv、atlassian-python-api、requests，确保脚本能自动读取 .env。
- 规则：
  - 创建完 Bug 必须附上 Jira 链接回复。
  - 海妖相关 Bug：修复版本默认填"海妖版本"；若不存在，回退未发布最高/最低版本。
  - normalize_version 白名单已加"海妖版本"，避免创建时报"版本名无效"。
  - X3NEW 新建界面暂不支持"归属功能"字段 customfield_12901，创建时留空，必要时事后在 Jira 内补充。

## 今天的工作

**2026-03-24**
1. 核查了世界入侵活动时间描述与实际不符的问题，确认是规则文本与配置表时间不一致，规则文本需更新。
2. 核查了海妖属性界面报错问题，定位到是代码绑定路径与预制体节点命名不一致，当前暂不处理。
3. 确认了世界入侵规则文本的修改方案。
4. 创建了4条海妖 Bug，涵盖属性界面底部缺失背景遮罩、技能预览界面缺失背景、自有属性点显示负数、技能数值万分比未转百分比，等级C，经办人 liuweiwei，修复版本海妖版本。
5. 修复了 Jira 创建脚本的 bug，版本"海妖版本"不会被错误标准化。

## 历史 Bug 记录（X3NEW）

- X3NEW-145：【海妖】属性界面底部文本缺失背景遮罩（C，liuweiwei，海妖版本）。
- X3NEW-146：【海妖】技能预览界面底部UI背景缺失（C，liuweiwei，海妖版本）。
- X3NEW-147：【海妖】使用任意技能属性点后，自由属性点显示为负数（C，liuweiwei，海妖版本）。
- X3NEW-148：【海妖】技能数值显示为万分比原值，未转化为百分比（C，liuweiwei，海妖版本）。

## 代码排查积累

- 世界入侵：RuleTips.xlsx ID=1901 规则文本错，实际配置 startTime+120h 起算、6场、UTC 00/04/08/12/16/20、每场约2h。
- 海妖 UIMechaAttributeEntry 空引用：Auto_UIMechaAttributeEntry.cs 绑定路径与预制体节点命名不一致，暂不处理。

## 其他

- 确认：以后创建 Bug 无需再确认，直接创建并贴链接；用户会在 Jira 内自行修订字段。
