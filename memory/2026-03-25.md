# 2026-03-25 日志

## 早间

- 早间播报 cron 触发时，MiniMax-M2.7 模型 503 报错（bi 分组无可用渠道），自动 fallback 到 gpt-5 完成播报
- 播报内容：代码排查分区昨日新增世界入侵规则文案修复、海妖属性界面空引用暂不处理、海妖 Bug 批量创建（X3NEW-145~148）

## cron 配置修复

- daily-memory-archive 提交信息从固定 'daily: 2026-03-19' 改为动态 'daily: $(date +%F)'
- 同时加入编辑失败回退策略说明：精确替换未命中时回退为读全文→追加→write 覆盖

## jira-bug-creator 附件规则（重要）

- `create_bug(attachment=...)` 内部会自动上传第一张附件，**不要再额外调用 upload-attachment.py 上传同一张图**，否则会重复
- 多图处理：`attachment` 传第一张，第二张起用 `upload-attachment.py` 逐个补传
- 一条消息里的所有截图全部上传到同一个 Bug 单

## jira-bug-creator 重大改进

### 防重复创建机制
- run-bug.py 新增 `--dry-run` / `--confirm` 双参数防护
- 默认不加参数只展示不创建，必须显式加 `--confirm` 才真正提单
- 失败时 sys.exit(1)，不再静默退出

### 执行环境确认
- `uv run` 默认使用系统 Python（/usr/bin/python3），不含 atlassian 包
- 正确执行路径：`/root/.cache/uv/environments-v2/run-bug-0c39fa0988fbec23/bin/python scripts/run-bug.py`
- 上传附件：`/root/.cache/uv/environments-v2/upload-attachment-2f50c7fcc10674f3/bin/python scripts/upload-attachment.py`

### Bug 创建流程改为子 agent
- 哥要求：收到 bug 描述后直接 spawn 子 agent 执行，主 agent 不等待，可连续接收新 bug
- 子 agent 创建完后通过 message 工具直接发结果到 hub-channel
- 已更新 MEMORY.md 全局工作规范

### 重复单事故
- 原因：调试阶段反复执行 run-bug.py，每次都真正连接 Jira 创建了单
- 创建了 X3NEW-152~158 共 7 个重复单（哥手动删除）
- 最终保留 X3NEW-159（海妖排行榜头像未适配圆形方框底板）

## 今日 Bug 单

| 单号 | 概要 | 等级 | 状态 |
|---|---|---|---|
| X3NEW-159 | 【海妖排行榜】头像未适配圆形方框底板 | C | 已创建+附件 |
| 待确认 | 【海妖】确认点数分配后海妖展示视频消失 | B | 子 agent 处理中 |
| 待确认 | 【海妖】点击补充气血按钮无反馈 | B | 子 agent 处理中 |
| 待确认 | 【海妖】分享窗口海妖头像未适配 | C | 子 agent 处理中 |

## 系统设计讨论：怪谈题材融合 SLG 立项

- 题材：买量钩子"家人敲门/判断门外是人还是怪物"，私密室内场景
- 开场玩法对标 Last Asylum：俯视角房间为主视角，点击大门进入独立第一人称小游戏判断真假，判断完退回俯视角
- 收留对象从"家人"扩展为"幸存者"解决数量上限问题，但买量素材保持"家人敲门"不变
- 房间体系 = SLG 城建等比缩放：客厅（主堡）→ 卧室（民居）→ 厨房（农场）→ 武器间（兵营）→ 瞭望台（侦察塔）
- 过渡叙事：怪物成群结队来了，单靠一家守不住 → 联合幸存者据点 → SLG 大地图
- 后续可扩展的小游戏类型：夜间巡逻手电筒扫描、声音方向判断、物资检查验证身份等

## 其他

- 哥问了 Unity 预制体变体制作方式（Project 窗口右键 → Create → Prefab Variant）
- 当前主对话模型：onehub-claude/claude-sonnet-4-6
- MiniMax-M2.7 在 bi 分组下无可用渠道，早间播报不适合用该模型

## 日报格式备注（哥反馈）

- bug 提交类工作只需一句话概括（如"测试了海妖相关界面并提交了若干 bug"），不需要逐条列出
- 怪谈立项讨论、Unity 技术问答等内容也要纳入日报

## 自我复盘（2026-03-25）

- **日记漏写根因**：日记在 14:43 heartbeat 时写入一次后未再补写，下午发生的怪谈立项讨论、预制体问答全部漏掉
- **改进**：日报触发前（21:00）必须先回顾当天完整对话，补全日记后再生成日报；不能依赖 heartbeat 的单次写入
- **日报时间**：已从 21:15 改为 21:00
